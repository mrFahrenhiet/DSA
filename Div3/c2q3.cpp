#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main() {
	#ifndef ONLINE_JUDGE
    // for getting input from input.txt
    freopen("input.txt", "r", stdin);
    // for writing output to output.txt
    freopen("output.txt", "w", stdout);
    #endif
    int t;
    cin>>t;
    int boys[200001];
    int girls[200001];
    while(t--) {
        int a,b,k;
        cin>>a>>b>>k;
        for(int i=0;i<k;i++) cin>>boys[k];
        for(int j=0;j<k;j++) cin>>girls[k];
        
    }
    return 0;

}